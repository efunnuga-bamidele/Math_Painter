Title: Math Painter

Description: An app that collects dimensions, color choice, shape type, and coordinate from the user, draws a canvas and generates a PNG file of the canvas for the user


Objects: Square:
            x_axis
            y_axis
            side
            color
            draw(canvas)
         Rectangle:
            x_axis
            y_axis
            width
            height
            color
            draw(canvas)
         Canvas:
            width
            height
            color
            make(imagepath)